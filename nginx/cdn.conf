# nginx/cdn.conf
server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;

  location / {
    try_files $uri =404;
    expires 365d;
    add_header Cache-Control "public, immutable";
  }

  # keine Skript-Ausführung
  location ~* \.(php|pl|py|sh|cgi)$ { return 403; }
}
