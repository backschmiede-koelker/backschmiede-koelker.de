services:
  backschmiede-cdn:
    image: nginx:alpine
    container_name: backschmiede-koelker-cdn
    restart: unless-stopped
    ports:
      - "3001:80"
    volumes:
      - ./uploads:/usr/share/nginx/html:ro
      - ./nginx/cdn.conf:/etc/nginx/conf.d/default.conf:ro
